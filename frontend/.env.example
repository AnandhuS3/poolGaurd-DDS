# =============================================================================
# Frontend Environment Variables
# =============================================================================
# Copy to .env.local for local overrides (gitignored).
# On Railway, set these as Service Variables in the dashboard.

# URL of the deployed backend service.
# Dev default: leave empty — Vite proxy handles it via localhost:8000
# Production: https://your-backend.up.railway.app
VITE_API_URL=
