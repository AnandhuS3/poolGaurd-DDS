# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9
websockets==12.0

# Computer Vision and ML
# opencv-headless: no GUI/display deps needed on a server (avoids libGL.so.1 error)
opencv-python-headless==4.9.0.80
ultralytics==8.1.11  # YOLOv8 detection + YOLOv8-pose for pose estimation
torch==2.1.2  # Required for YOLO and LSTM temporal classifier
torchvision==0.16.2

# Pose Estimation (Optional - for alternative pose backend)
# mediapipe>=0.10.0  # Uncomment to use MediaPipe instead of YOLOv8-pose

# Object Tracking
deep-sort-realtime==1.3.2
numpy==1.26.3

# Video Download
yt-dlp>=2023.12.30

# Utilities
pydantic==2.5.3

# Authentication & Database
mysql-connector-python==8.2.0
PyJWT==2.8.0
bcrypt==4.1.2
python-jose[cryptography]==3.3.0
pydantic[email]==2.5.3
email-validator==2.1.0

# Email domain validation (MX record lookup)
dnspython>=2.4.0

# Timezone Support (India - IST)
pytz==2024.1

# Environment Variables (Secure Credentials)
python-dotenv==1.0.0
